import React from 'react'

const Header = () => {
    return(
        <div className="text-center font-weight-bold h1 bg-primary py-4">
            Employees Details
        </div>
    );
}

export default Header;